# Flash size — pin it explicitly. ESP-IDF defaults to 2 MB; if the board is larger
# the auto-generated partition table can land wrong, which shows up as an MMU
# alignment assert in `mmu_hal_map_region` while loading the partition table at
# boot. 4 MB is safe even on this 8 MB board (under-reporting is fine; over-
# reporting is not) and the slim app fits comfortably.
CONFIG_ESPTOOLPY_FLASHSIZE_4MB=y
CONFIG_ESPTOOLPY_FLASHSIZE="4MB"

# Main-task stack. The main task now only does startup (WiFi + SNTP + spawning),
# NOT the deep async work: the iroh/tokio runtime runs on a dedicated thread whose
# stack lives in PSRAM (see run_iroh() in main.rs). That's the key to fitting relay
# + the CO2 sensor on the C61's ~320 KB SRAM (~188 KB heap): the deep relay/QUIC/TLS
# stack (~84 KB) can't share scarce INTERNAL SRAM with the relay's FreeRTOS
# mutexes/DMA/TCBs (none PSRAM-backable), so we move it OUT of internal SRAM. 32 KB
# is ample for the shallow startup path and frees ~65 KB of internal SRAM vs 96 KB.
CONFIG_ESP_MAIN_TASK_STACK_SIZE=32768

# Use this to set FreeRTOS kernel tick frequency to 1000 Hz (100 Hz by default).
# This allows to use 1 ms granularity for thread sleeps (10 ms by default).
#CONFIG_FREERTOS_HZ=1000

# We use rustls, not mbedtls - disable to save RAM and flash
CONFIG_MBEDTLS_CERTIFICATE_BUNDLE=n
CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_DEFAULT_FULL=n
CONFIG_MBEDTLS_TLS_ENABLED=n

# Enable PSRAM. Unlike the C6 (no PSRAM interface at all), the ESP32-C61 has a
# 2 MB SPI PSRAM. The gate is the chip-independent CONFIG_SPIRAM symbol — the
# esp32-only CONFIG_ESP32_SPIRAM_SUPPORT alias does NOT exist for this RISC-V
# part and would be silently ignored → PSRAM never inits → back to internal SRAM.
# The C61's PSRAM is QUAD (the ESP-IDF default), so we do NOT set MODE_OCT — that
# is the S3's octal-part knob and would fail to init here.
CONFIG_SPIRAM=y
CONFIG_SPIRAM_USE_MALLOC=y
CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL=0
CONFIG_SPIRAM_MALLOC_RESERVE_INTERNAL=0
# Allow task stacks in PSRAM — frees internal SRAM for FreeRTOS/lwIP.
CONFIG_SPIRAM_ALLOW_STACK_EXTERNAL_MEMORY=y
# Push WiFi + lwIP buffers into PSRAM (DMA-required parts fall back to internal).
# The plain c61-psram relay build fit in internal SRAM, but adding the CO2 sensor
# thread + I2C/SPI drivers tipped it over: the relay's DNS path uses spawn_blocking
# → tokio creates a blocking worker thread, and its FreeRTOS TCB/stack (which can't
# live in PSRAM) then failed with ENOMEM. Relocating WiFi/lwIP frees enough internal
# SRAM to restore the headroom.
CONFIG_SPIRAM_TRY_ALLOCATE_WIFI_LWIP=y

# WiFi buffers — CONFIG_ESP_WIFI_* are the canonical, chip-independent symbols.
# The slim LAN-direct echo doesn't need throughput; cut static buffers to the floor
# (these are internal SRAM, pre-allocated). With PSRAM these cuts just leave
# headroom, but there's no reason to grow them for an echo.
CONFIG_ESP_WIFI_STATIC_RX_BUFFER_NUM=4
CONFIG_ESP_WIFI_STATIC_TX_BUFFER=y
CONFIG_ESP_WIFI_STATIC_TX_BUFFER_NUM=4
CONFIG_ESP_WIFI_DYNAMIC_RX_BUFFER_NUM=8
CONFIG_ESP_WIFI_AMPDU_RX_ENABLED=n
CONFIG_ESP_WIFI_AMPDU_TX_ENABLED=n

# lwIP buffers — small. This variant is LAN-direct (no relay/pkarr → no TCP): QUIC/
# SNTP/DNS are all UDP. Floor the TCP windows for hygiene; keep UDP mbox moderate
# so QUIC rx bursts aren't dropped.
CONFIG_LWIP_UDP_RECVMBOX_SIZE=12
CONFIG_LWIP_TCP_SND_BUF_DEFAULT=2880
CONFIG_LWIP_TCP_WND_DEFAULT=2880
CONFIG_LWIP_TCPIP_RECVMBOX_SIZE=8
CONFIG_LWIP_TCPIP_TASK_STACK_SIZE=4096
CONFIG_LWIP_MAX_UDP_PCBS=6
CONFIG_LWIP_MAX_SOCKETS=6

# IPv6 off: IPv4-only home/factory WLAN. iroh handles the v6 bind failure
# gracefully ("ignoring non required bind failure").
CONFIG_LWIP_IPV6=n

# Use the ROM's nano printf/scanf: smaller formatting code AND a lower stack
# footprint on the format path.
CONFIG_NEWLIB_NANO_FORMAT=y

# Restore colored log output. ESP-IDF flipped CONFIG_LOG_COLORS to default-off
# between v5.3 (the C6, colored) and v5.5 (this target). Turn it back on.
CONFIG_LOG_COLORS=y

# Detect stack overflow via hardware watchpoint (uses 1 of 2 debug watchpoints)
CONFIG_FREERTOS_WATCHPOINT_END_OF_STACK=y
